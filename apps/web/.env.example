# L'interface ne connaît aucune URL d'API : tout passe par /api sur sa propre
# origine, routé vers le BFF par nginx. Ce fichier n'a donc rien à déclarer
# aujourd'hui — les adresses des produits vivent dans apps/api/.env.
